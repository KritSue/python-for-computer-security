# Lab 05 — Failed login counter

Run `python examples/failed_login_counter.py`. The script counts failed events per synthetic username with a dictionary.

**Task:** set a review threshold of three events and print only usernames that meet it. Do not call the accounts compromised; a threshold is a simple alerting rule that needs human context.
