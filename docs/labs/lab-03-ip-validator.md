# Lab 03 — IP address validator

Run `python examples/ip_validator.py`. The program parses a list containing valid IPv4, valid IPv6, and invalid sample strings.

**Task:** add another made-up candidate and report its address version. Explain why “valid format” does not mean “reachable.” The parser handles correctness better than splitting on dots by hand.
