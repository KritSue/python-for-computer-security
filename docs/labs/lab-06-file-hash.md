# Lab 06 — SHA-256 file hash

Run `python examples/file_hash.py examples/data/sample.log`. The script reads bytes and prints a SHA-256 digest.

**Task:** make a copy of the sample file, change one character, and compare its digest. This is an integrity exercise. SHA-256 alone is not a password-storage method and a digest is not encryption.
