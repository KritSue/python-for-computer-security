# Lab 07 — JSON security event parser

Run `python examples/json_event_parser.py`. The input file is `examples/data/events.json` and contains synthetic records.

**Task:** count events by severity, safely handle a missing severity field, and report a malformed record. JSON parsing converts objects to dictionaries and arrays to lists.
